# Backend (Supabase)

Схема базы данных (profiles, projects, datasets, buildings, nodes, pipes, calc_runs, exports), политики RLS, конфигурация Auth и Storage появятся в Фазе 2.

Структура:

- `migrations/` — SQL-миграции схемы БД
- `policies/` — политики Row Level Security
