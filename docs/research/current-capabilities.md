# Текущие возможности AquaScheme

Статусы:

- **Реализовано** — существует рабочий код и локальная автоматическая проверка.
- **Частично** — основной путь есть, но отсутствует важная интеграция, проверка или тип исходных данных.
- **Зависит от окружения** — код есть, но нужен внешний сервис/схема.
- **Макет/заглушка** — интерфейс или контракт объявлен, реальной функции нет.

## Пользовательский контур

| Возможность | Статус | Основание и ограничение |
| --- | --- | --- |
| Email/password auth | Реализовано | Supabase Auth, route guard и session provider. Реальный внешний login в аудите не выполнялся. |
| Проекты В1/К1/К2, новое строительство/реконструкция | Реализовано | CRUD в `projects`, условный UI по `system_type` и `work_type`. |
| Приватные исходники и экспорты | Зависит от окружения | Storage и RLS описаны SQL; production bucket/policies не проверялись. |
| RU/KZ/EN | Реализовано | Три полных словаря i18next. Есть устаревшие тексты о состоянии K1. |
| Индикация длительных операций | Реализовано | Export/demo имеют spinner, elapsed time, блокировку повторных кликов; не весь UI использует единый timeout/cancel abstraction. |

## Импорт и модель исходных данных

| Возможность | Статус | Основание и ограничение |
| --- | --- | --- |
| CSV зданий | Реализовано | Парсер, валидация и unit-тесты. |
| XLSX/CSV геологии | Реализовано | Настраиваемое сопоставление колонок, проверка слоёв/скважин. |
| Цифровой PDF геологии | Частично | pdfjs извлекает текст и таблицы; пользователь обязан проверить. Скан без text layer не распознаётся. |
| GeoJSON сети | Реализовано | Извлечение линий/точек и импорт топологии. |
| DXF сети и топосъёмки | Реализовано | `dxf-parser`, классификация слоёв, сегменты, точки, отметки и аудит. Корни ролей рассчитаны на обрезанные до 10 символов имена муниципальных основ; отметки берутся из текстовых подписей, когда геометрия плоская, с явным `surveyPointSource`. |
| Реконструкция по съёмке | Реализовано частично | `buildReconstructionFromSurvey` собирает сеть, профиль, ведомость и пересечения из одной топоосновы; есть секция в UI. Результат пока не сохраняется в проект, поэтому дальше по конвейеру не уходит. |
| DWG | Зависит от окружения | Frontend вызывает converter DWG→DXF. Без `VITE_CONVERTER_URL` и готового ODA импорт невозможен. |
| CRS/геопривязка | Частично | Поддерживаются proj4/control points/local anchor; при отсутствии блокируется строгая трасса. Default anchor привязан к Астане и небезопасен как универсальный fallback. `detectSurveyGrid` читает координатную сетку чертежа: круглый шаг и осевые штрихи доказывают метрику, масштаб и отсутствие разворота, а подписанные линии дают и начало координат. Датум/EPSG из сетки не следует. |
| ТЗ, АПЗ, ПДП, ТУ, генплан PDF | Частично | Бинарники сохраняются как basis-файлы, но их требования/геометрия автоматически не извлекаются. |
| Полная provenance-модель | Частично | Есть `file_name`, `meta`, `dataSource`, `sourceLayer`, source cards и hash; нет единого статуса происхождения каждого значения. |

## Инженерное ядро

| Возможность | Статус | Основание и ограничение |
| --- | --- | --- |
| Водопотребление/расходы | Реализовано | Суточные/часовые коэффициенты, пожарный расход, напор по этажности; defaults требуют подтверждения. |
| Напорная гидравлика | Реализовано | EPANET WASM, Web Worker, sizing loop, давления/потери/скорости. |
| Эвристическая кольцевая сеть В1 | Реализовано как fallback | `traceNetwork` строит геометрическую кольцевую/поперечную сеть и прямые участки без полной модели препятствий; это не строгий генпланный проектировщик. |
| Строгая трасса К1/К2 | Реализовано | A*-подобная grid routing, коридор, hard obstacles, красные линии, сети, дороги, гидрография, рельеф, approved water crossings, source declarations. |
| Утверждённая ось генплана | Частично | Guide-линии влияют на стоимость, а импортированная фактическая alignment сохраняется; нет формального режима неизменяемой утверждённой оси с change-order workflow. Для реконструкции ось восстанавливается из разметки съёмки (`extractExistingUtilities`): пары отметок «крышка/лоток» дают цепочку колодцев. Для нового строительства ось выводится из коридора (`corridorAxis`): замкнутый контур распознаётся как узкая полоса по 2·площадь/периметр, осевая линия строится сопоставлением противоположных сторон. На исходном проекте — 50 вершин, 16 324 м против 15 793 м фактических (3,4%). Это предложение, а не утверждённая ось: подтверждает инженер. |
| Контроль полноты маршрута | Реализовано | Блокеры для отсутствующих facilities, flows, corridor, terrain, CRS, классификации и наборов препятствий. |
| Самотёчная гидравлика К1/К2 | Реализовано | Manning, накопление расходов, диаметр, заполнение, скорость, уклон, профиль и ведомость. Ограничения по промерзанию до лотка и покрытию до верха трубы разделены; без подтверждённой глубины финальный профиль блокируется. |
| Дождевой сток К2 | Реализовано частично | Формулы 5.8–5.13, таблицы 5.9–5.12, площади/покрытия, время добегания, beta и строгая provenance‑проверка реализованы; `drainage.stormCatchments` подключены к гидравлике. Табличная экстраполяция запрещена, интерполяция трассируется, исключение скорости 0,6 м/с действует только при проверенном `P=0,33`. Нужны площадочный подтверждённый `q20` и отдельные подрасчёты неоднородных водосборов свыше 50 га. |
| Напорный участок ЛНС | Частично | Топология и отдельный pressure main есть; отсутствие pump duty блокирует окончательный статус. Полного подбора насосного оборудования/режимов в видимом контуре нет. |
| Геология по трассе | Реализовано частично | Интерполяция скважин, УГВ, агрессивность и влияния; полноценная пространственная геомодель/геостатистика отсутствует. |
| Вертикальная планировка | Реализовано частично | Есть blending проектной и существующей поверхности; импорт/распознавание полного чертежа вертикальной планировки не автоматизирован. |
| Пересечения | Частично | Геометрическое обнаружение и карточки реализованы; отметки, владелец, согласование и метод должны быть заполнены, иначе выпуск блокируется. `crossingsFromSurvey` строит карточки из съёмки: пикет, вид сети по слою и отметка из нивелирной подписи той же сети; многоштриховый символ схлопывается, колодцы собственной трассы исключаются. Владелец, способ и согласование съёмкой не покрываются и остаются пустыми. |
| Земельные участки/полоса отвода | Реализовано | Полигоны, привязка зданий и проверки нарушений. Правовые решения остаются за инженером. |
| Материалы и арматура | Реализовано | Встроенный или загруженный каталог, подбор материала, fittings, колодцы. |
| ВГСК | Макет/заглушка | Контракт есть, `available: false`, build бросает исключение. |
| CloudConvert | Макет/заглушка | Provider объявлен, но readiness и convert возвращают not implemented. |

## Нормативная база

| Возможность | Статус | Основание и ограничение |
| --- | --- | --- |
| Реестр документов и пунктов | Частично | 73 правила: 45 verified, 28 unverified, 16 без точного пункта. |
| Привязка решений к refs/basis | Реализовано частично | `Justified<T>`, decision log, badges и sources существуют; не каждый элемент каждого листа имеет полную цепочку до страницы источника. |
| Конфликты редакций | Частично | Документированы известные конфликты 2011/2013 и статус ГОСТ Р 21.101; автоматической иерархии для всех кодексов/редакций нет. |
| Проверка актуальности на дату проекта | Не подтверждена | Нет автоматического источника обновлений или lock-файла нормативной редакции проекта. |

## Карта и визуализация

| Возможность | Статус | Основание и ограничение |
| --- | --- | --- |
| Настоящая OSM-подложка | Реализовано | Leaflet, zoom/pan, подписи трассы и контрольных точек. |
| Esri/OpenTopoMap | Реализовано в переключателе | Доступность зависит от внешних tile endpoints. |
| Слои трассы/полосы отвода | Реализовано | Отдельные стили и legend. |
| Рельеф карты | Частично | Высоты берутся из survey, не из OSM. Полная DEM/горизонтали реального объекта зависят от импортированных данных. |
| Редактирование инженерной трассы | Частично | Можно перестроить расчёт; полноценного CAD-редактора узлов/кривых/согласований нет. |

## Документы и экспорт

| Возможность | Статус | Основание и ограничение |
| --- | --- | --- |
| DXF плана сети | Реализовано | Векторный генератор и тесты структуры. |
| DXF профиля | Реализовано | Пикетаж, отметки, уклоны, диаметры и нарезка. |
| PDF пояснительной записки | Реализовано | pdfmake, расчётные и нормативные разделы. |
| Формы актов PDF | Реализовано | Набор нормативных форм; не проверен визуально против всех исходных приложений в этом аудите. |
| XLSX спецификация/ведомости | Реализовано | Трубы, колодцы, компоненты и provenance каталога. |
| ZIP-комплект | Реализовано | fflate собирает выбранные документы. |
| DWG export | Зависит от окружения | DXF→DWG только через готовый converter. При недоступности остаётся DXF. |
| Data-driven рабочий альбом | Реализовано | Векторный PDF, три общие страницы, динамические планы, отдельный сводный `network_plan`, профили, ведомости, детали и пагинируемая отдельная серия спецификации; действует status gate. Плановые интервалы используют канонический пикетаж профиля, таблицы — явные непересекающиеся диапазоны строк. |
| Структурный контракт 61 страниц R01 | Реализовано | Нейтральный каталог проверяет последовательность, типы, размеры, ориентации, источники и unresolved-поля каждой страницы; это не visual diff и не источник проектных значений. |
| Полные 61 страницы эталона | Не реализовано | Динамический состав приблизился к наблюдаемым семействам листов, но физические форматы, основная надпись и графика ещё не прошли автоматическое постраничное сравнение. |
| Основная надпись по эталону | Частично | Есть простой footer/заголовок, но не доказано соответствие полной форме и всем реквизитам ГОСТ. |
| Visual diff 99% | Реализован механизм, реальный результат не измерен | `benchmark:visual` рендерит оба PDF, проверяет число страниц, считает pixel/ink/structure score по каждому листу и создаёт JSON/HTML/Markdown плюс reference/generated/diff PNG; самотест одинаковых PDF даёт 100%. Без сгенерированного 61‑листового контрольного альбома честного процента R01 пока нет. |

## Backend и deployment

| Возможность | Статус | Основание и ограничение |
| --- | --- | --- |
| RLS и owner-only RPC | Реализовано в SQL | Нужна проверка реально применённой production-схемы. |
| Атомарная замена сети | Реализовано | Одна PostgreSQL function, rollback при ошибке. |
| Сохранение gravity result | Реализовано | RPC обновляет трубы и создаёт `calc_runs`. |
| Идемпотентный полный bootstrap | Не реализован корректно | `0000_full_schema.sql` не включает большую часть последующих миграций. |
| Автоматические миграции | Не реализовано | Нет Supabase CLI config/CI deploy. |
| Converter liveness/readiness | Реализовано и проверено для production endpoint | `/health`, `/ready` и синтетический DXF→DWG→DXF round-trip успешны; локальный ODA отсутствует, конфиденциальный P04 во внешний endpoint не передавался. В коде добавлена fail‑closed CORS allowlist, но новые env/деплой Render ещё не проверены. |

## Тестируемость

- Исходный baseline: engine — 53 файла/311 тестов, frontend shared export — 2 файла/6 тестов.
- Текущая рабочая копия добавляет 61 постраничный контракт, проверки `network_plan`, канонического пикетажа, диапазонов таблиц/спецификации, строгой защитной сетки, расчёта дождевого стока и visual-score.
- Корневая команда `npm run test:all` запускает engine, все frontend unit-тесты, converter CORS-тесты и самотест метрики visual benchmark последовательно.
- TypeScript frontend/engine — успешно.
- Production build — успешно.
- Нет браузерного E2E, coverage threshold, реального ODA round-trip, Supabase integration и полного benchmark на паре 61-листовых PDF.
