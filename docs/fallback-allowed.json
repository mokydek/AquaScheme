{
  "engine/src/basin-links.ts | parallelCount: input.parallelCount ?? 1,": "Перемычка в одну нитку по определению.",
  "engine/src/contours.ts | const spanX = maxX - minX || 1": "Защита деления на вырожденном пролёте: ноль по оси означает отсутствие протяжённости, и результат всё равно пуст.",
  "engine/src/contours.ts | const spanY = maxY - minY || 1": "Защита деления на вырожденном пролёте.",
  "engine/src/contours.ts | lines: [], stepM: options.stepM ?? 0, zMinM: 0, zMaxM: 0,": "Эхо параметров в ПУСТОМ результате: горизонталей нет, и рядом стоит `reason`.",
  "engine/src/contours.ts | triangles: 0, skippedTriangles: 0, maxEdgeM: options.maxEdgeM ?? 0, reason,": "Эхо параметров в пустом результате.",
  "engine/src/crossing-triage.ts | const minSamples = options.minSamples ?? 2": "Параметр отбора, а не инженерная величина: сколько замеров считать достаточным для вывода.",
  "engine/src/crossings-from-survey.ts | const labelRadius = options.labelRadiusM ?? 6": "Радиус поиска подписи вокруг точки: параметр разбора чертежа.",
  "engine/src/existing-condition.ts | const system = input.system ?? 'sewer'": "Модуль о самотёчной канализации.",
  "engine/src/geology.ts | const window = text.slice(anchor.index ?? 0, (anchor.index ?? 0) + 900)": "Индекс совпадения регулярного выражения, не величина.",
  "engine/src/hydraulics.ts | const lengthM = lengthById.get(id) ?? 0": "Поиск по собственной карте длин.",
  "engine/src/hydraulics.ts | warnings.push({ code: 6, message: match[1] || 'System has negative pressures.' })": "Текст сообщения решателя: оформление, а не инженерная величина.",
  "engine/src/norms/drop-wells.ts | : `перепад ${round2(dropM)} м на трубе Ø${diameterMm || '—'} мм: слив в смотровом колодце не подходит `": "Прочерк в тексте сообщения: отсутствие диаметра уже выведено как «—», а не как число.",
  "engine/src/norms/drop-wells.ts | const slope = (pipeId ? design.get(pipeId)?.slope : undefined) ?? 0": "Поиск по собственной карте подбора.",
  "engine/src/norms/gravity.ts | chainageM: Math.round((headChainage - (distFromOutlet.get(nodeId) ?? 0)) * 100) / 100,": "Поиск по собственной карте подбора.",
  "engine/src/norms/gravity.ts | const dd = design.get(edge.pipeId)?.diameterMm ?? 0": "Поиск по собственной карте подбора.",
  "engine/src/norms/gravity.ts | const fill = fillForFlow(Q, Dm, Math.min(slope, SLOPE_CAP), n, 0.99) ?? 0.99": "То же, что строка 234: сигнальное «не помещается».",
  "engine/src/norms/gravity.ts | const fill = fillForFlow(Q, Dm, slope, n) ?? 0.99": "Сигнальное значение: `fillForFlow` не находит наполнения, когда расход не помещается в трубу. 0,99 ведёт к замечанию overMaxFilling, то есть к отказу, а не к принятому наполнению.",
  "engine/src/norms/gravity.ts | const flow = buildingFlowLps.get(node.buildingId ?? node.id) ?? node.designFlowLps ?? 0": "Узел без здания даёт нулевой приток по определению: это не подстановка, а отсутствие потребителя.",
  "engine/src/norms/gravity.ts | const flowLps = flows.get(p.id) ?? 0": "Поиск по собственной карте расходов решателя: ключа нет — участок не участвует в этом прогоне.",
  "engine/src/norms/gravity.ts | const groundSlope = Math.max(opts.groundSlope ?? 0, 0)": "Ноль означает «рельеф не учитывать», а не «уклон местности равен нулю»: величина служит нижней границей подбора и в результат не попадает. Конвейер всегда передаёт её явно.",
  "engine/src/norms/gravity.ts | const headChainage = distFromOutlet.get(head) ?? 0": "Поиск по собственной карте расстояний решателя: ключа нет — узел не в этом дереве.",
  "engine/src/norms/gravity.ts | const len = network.pipes.find((p) => p.id === edge.pipeId)?.lengthM ?? 0": "Поиск по собственной карте подбора.",
  "engine/src/norms/gravity.ts | const level = opts.level ?? 'street'": "Уровень сети: проект делает наружные уличные сети, внутриквартальные не проектируются. Дефолт — определение области применения, а не подмена данных.",
  "engine/src/norms/gravity.ts | const material = opts.material ?? 'nonmetal'": "Материал трубы для предела скорости: неметалл даёт БОЛЕЕ СТРОГИЙ предел (п. 5.10.3), то есть дефолт в сторону запаса.",
  "engine/src/norms/gravity.ts | const normMin = minSlopeForDiameter(D)?.value ?? 0": "То же, что строка 201.",
  "engine/src/norms/gravity.ts | const slope = design.get(pipeId)?.slope ?? 0": "Поиск по собственной карте подбора.",
  "engine/src/norms/gravity.ts | const slope = design.get(profile.pipeIds[i - 1])?.slope ?? 0": "Поиск по собственной карте подбора.",
  "engine/src/norms/gravity.ts | const slope = design.get(profile.pipeIds[index - 1])?.slope ?? 0": "Поиск по собственной карте подбора: участка нет — вклад в перепад нулевой.",
  "engine/src/norms/gravity.ts | const slope = pipeId ? design.get(pipeId)?.slope ?? 0 : 0": "Поиск по собственной карте расстояний.",
  "engine/src/norms/gravity.ts | slope: Math.round(Math.max(groundSlope, minSlopeForDiameter(adopted)?.value ?? 0, 0.0005) * 1e5) / 1e5,": "`minSlopeForDiameter` возвращает null там, где норма наименьшего уклона прямо не задаёт; ноль означает «нормой не ограничено» и участвует в Math.max как нейтральный член.",
  "engine/src/norms/masterplan.ts | const designLines = d.parallelLines ?? 1": "Труба в одну нитку по определению.",
  "engine/src/norms/masterplan.ts | const planLines = p.parallelLines ?? 1": "Труба в одну нитку по определению.",
  "engine/src/norms/masterplan.ts | designLines: d.parallelLines ?? 1,": "Труба в одну нитку по определению.",
  "engine/src/norms/masterplan.ts | planLines: p.parallelLines ?? 1,": "Труба в одну нитку по определению: отсутствие числа ниток означает одну, а не неизвестность.",
  "engine/src/norms/pumps.ts | : row.standby ?? 0": "Пустая графа таблицы нормы означает «резерв не требуется», а не «неизвестно»: строка таблицы прочитана целиком.",
  "engine/src/norms/pumps.ts | const spareOnStore = category === 'third' ? row.thirdCategorySpare ?? 0 : 0": "Пустая графа таблицы нормы для категорий, кроме третьей.",
  "engine/src/norms/pumps.ts | return justified({ standby, spareOnStore: row.spare ?? 0 }, refs, 'normative',": "Пустая графа таблицы нормы: склад без запасного агрегата — это ноль, а не пробел.",
  "engine/src/reconstruction-from-survey.ts | const system = options.system ?? 'sewer'": "Модуль о самотёчной канализации: другой системы у него не бывает.",
  "engine/src/reconstruction.ts | const length = segment.lengthM || 0": "Суммирование длин по участкам: отсутствие длины даёт нулевой вклад в сумму, и рядом считается число неполных строк.",
  "engine/src/sizing.ts | const idx = idxByPipe.get(p.id) ?? 0": "Индекс в собственной карте подбора.",
  "engine/src/sizing.ts | const pressure = res.nodes.get(j.id)?.pressureM ?? 0": "Поиск по собственной карте результата решателя.",
  "engine/src/sizing.ts | const required = requiredPressureFor(junction.id) ?? 0": "Требуемое давление отсутствует у узлов без требования: ноль означает «требования нет».",
  "engine/src/sizing.ts | const size = sizes[idxByPipe.get(p.id) ?? 0]": "Индекс в собственной карте подбора.",
  "engine/src/sizing.ts | const velocity = result.pipes.get(p.id)?.velocityMs ?? 0": "Поиск по собственной карте результата решателя.",
  "engine/src/sizing.ts | internalDiameterMm: sizes[idxByPipe.get(p.id) ?? 0].internalMm,": "Индекс в собственной карте подбора: отсутствие ключа означает первую ступень ряда, с которой подбор и начинается.",
  "engine/src/surveygrid.ts | const layer = segment.layer ?? '0'": "«0» — настоящее имя слоя по умолчанию в формате DXF, а не подставленная величина.",
  "engine/src/surveygrid.ts | const tickAngle = modal(ticks.map((t) => Number(t.angle.toFixed(1))), 0.1) ?? 0": "Угол штрихов при неопределимой моде: сетка тогда не признаётся найденной, и `detected` остаётся ложным.",
  "engine/src/surveygrid.ts | return { dx: dx ?? 0, dy: dy ?? 0 }": "Смещение по подписям сетки: отсутствие выражено отдельно полем `offsetSource: none`, и потребитель различает их по нему.",
  "frontend/src/shared/network.ts | p_route_status: route.status ?? 'calculated',": "Состояние трассы при сохранении: значение по умолчанию для состояния UI, инженерной величиной не является.",
  "frontend/src/shared/network.ts | throw new Error(parts.join(' · ') || 'Supabase отклонил сохранение инженерной сети.')": "Текст сообщения об ошибке."
}
